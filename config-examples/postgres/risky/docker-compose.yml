services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: root
      POSTGRES_PASSWORD: root
      POSTGRES_DB: playground
    ports:
      - "5432:5432"
    networks:
      - public

networks:
  public:
